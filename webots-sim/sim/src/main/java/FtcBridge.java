/** Default-package wrapper so Webots can find the controller by name. */
public class FtcBridge {
    public static void main(String[] args) {
        org.firstinspires.ftc.simulator.FtcBridge.main(args);
    }
}
